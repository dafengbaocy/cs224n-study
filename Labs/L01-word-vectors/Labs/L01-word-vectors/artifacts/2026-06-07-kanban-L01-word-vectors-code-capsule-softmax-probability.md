# CS224N L01-word-vectors Code Capsule — softmax-probability（WP03）

你是 CS224N code-capsule-runner。为本讲概念制作小而真实可运行的代码胶囊。

## 目标

- course: CS224N-2025
- lecture: L01-word-vectors
- waypoint: WP03
- concept: Softmax probability computation
- official anchor: slides p28-p30 (P(o|c)=exp(u_o^T v_c)/sum exp(u_w^T v_c)); notes §3.2 Eq.4-Eq.5

## 输入（只读这些，不读 AGENTS.md / flow docs）

- `Lectures/L01-word-vectors/02-readings-map.md`（WP03 部分）
- `Lectures/L01-word-vectors/02b-slides-notes-teaching-figures.md`（对应 anchor 小节）
- `Lectures/L01-word-vectors/00-concept-glossary.md`（Code 前置 seed，只链接已有 heading）

## 任务

1. 说明这个 capsule 解释哪个概念，为什么不能只靠文字：The denominator over the whole vocabulary is easy to gloss over in formulas; running it on a tiny vocabulary shows dot product scores, exponentials, normalization, and probability mass.
2. 写最小脚本（优先纯标准库，便于 Colab 无包运行）。
3. 真实运行，记录共享 run-log：timestamp、hostname/environment、command、exit code、stdout/stderr、输出路径、notebook 执行后路径。`Labs/L01-word-vectors/run-log.md` 是全讲共享日志，可以共享但不能无锁并发写。每个 capsule 必须写带 `task_id / capsule_slug / run_id` 的独立 section，并用 `flock` 原子追加，不能直接多进程 `>>`。
4. 保存输出（stdout/表/图/指标至少一种）。禁止编造 stdout。
5. 图表存 `Labs/L01-word-vectors/outputs/`，文件名含语义，说明生成 cell/输入/先看哪里/对应概念。
   - 本地输出图是识别/provenance 轨，必须保留。
   - 同时立即上传图床。优先用 `picgo-media` MCP `upload_image`；MCP 不可见时用 CLI：
     `/opt/hermes/.venv/bin/python /opt/data/scripts/picgo_media_mcp.py upload --path "<local_image_path>" --title "softmax-probability-<content_slug>"`
   - 上传失败不能只试一次就放弃。每张需要上传的本地图片必须最多尝试 **5 次**，建议退避等待 `5s / 10s / 20s / 40s / 60s`；可以在 MCP 和 CLI 之间切换。5 次后仍失败，才允许写 `upload_status: blocked`。blocked 图片必须登记 `upload_attempt_count: 5` 和 5 条 `upload_attempts`，每条包含 `attempt_no / method / command_or_tool / timestamp / result / error`。
   - `Lectures/L01-word-vectors/code-capsules/softmax-probability.md` 中显示图片时必须使用 `remote_url`，不能使用 `![[Labs/...]]`、`Assets/...`、`/workspace/...`、`/vol2/...`。
   - 凡写进用户正文的图片，必须写入 `Lectures/L01-word-vectors/media-registry/code-capsule-softmax-probability.json`。本地 outputs 中未嵌入正文的测试图、替代图、调试图可以只保留在 run-log/outputs 中，不强制上传；但不能在正文里引用本地路径。schema 必须是顶层 `items` 列表，禁止写旧格式 `assets`。示例：
     ```json
     {
       "lecture": "L01-word-vectors",
       "producer_stage": "code-capsule",
       "producer": "softmax-probability",
       "items": [
         {
           "asset_id": "softmax-probability-output-plot",
           "local_path": "Labs/L01-word-vectors/outputs/softmax-probability-output-plot.png",
           "remote_url": "https://...",
           "source_provenance": "generated by Labs/L01-word-vectors/softmax-probability.py in current run",
           "run_log": "Labs/L01-word-vectors/run-log.md#run_id <RUN_ID>",
           "used_for": ["WP03 Softmax probability computation"],
           "alt": "Softmax probability computation code output plot",
           "upload_status": "uploaded",
           "upload_attempt_count": 1,
           "upload_attempts": []
         }
       ]
     }
     ```
6. 生成 `.ipynb`，按「Colab 发布」发布到 GitHub 拿 Colab 链接（默认必做）。
7. 写 `Lectures/L01-word-vectors/code-capsules/softmax-probability.md` 单 capsule partial（稳定 heading，嵌入 Colab 链接）。**不要写总入口 `04-code-capsules.md`**。
8. 写入 Obsidian/LiveSync 后 `vault read` 验证。

## Notebook 中文教学规则（一票否决）

`.ipynb` 不是给 agent 自己看的运行副本，而是用户可能直接点进 Colab 阅读和实验的课堂材料。因此：

- notebook 中所有 Markdown cell 必须使用中文解释；必要英文术语可以保留，但必须紧跟中文解释，例如 `softmax（把分数归一成概率）`。
- 面向学习者的代码注释必须中文优先；API 名、变量名、库名可以英文，但注释要说清"这一行在课堂概念里对应什么"。
- 每个 notebook 至少包含这些中文小节：`这段代码在看什么`、`运行后先看哪里`、`输出怎么解释`、`和本讲哪个 waypoint 对应`、`容易误解的地方`。
- notebook 输出图/表旁边必须有中文读图说明：先看哪个轴/哪一列、哪个数值最关键、它支持哪个课堂结论。
- 禁止只把 `.py` 脚本 mechanically 转成 notebook；如果 Markdown cell 只是英文标题或英文 API 说明，Content Gate 必须 `BLOCK`。
- `Lectures/L01-word-vectors/code-capsules/softmax-probability.md` 与 notebook 的教学叙述要一致；不能正文中文、notebook 英文。

## Partial 写入规则（防并发覆盖）

多个 capsule 会并行生产，所以 worker 不允许写同一个总入口。

- 本任务唯一允许写入的学生正文是 `Lectures/L01-word-vectors/code-capsules/softmax-probability.md`。
- 禁止写、改、创建 `Lectures/L01-word-vectors/04-code-capsules.md`；它只能由 integration 阶段的 `merge_code_capsules.py` 自动合并生成。
- 禁止在 `Labs/L01-word-vectors/04-code-capsules.md`、`artifacts/` 或其他目录创建同名替代入口。
- 禁止修改其他 capsule 的 partial。
- 如果发现别的 capsule partial 缺失、重复、被覆盖、或出现旧 run 残留，不要静默修；在执行报告写 `integration_risk`。
- glossary 链接必须使用 vault-root 路径，例如 `[[Lectures/L01-word-vectors/00-concept-glossary#softmax|softmax]]`。跨目录时禁止短链 `[[00-concept-glossary#softmax]]`。
- `00-concept-glossary.md` 已由编排器在 Code Capsules 前生成 seed。Worker 不得创建、重写或并发修改这个共享文件。
- 只能链接 seed 中已经存在的 heading。若需要的新术语没有 heading，不要造坏链接；在执行报告写 `requested_glossary_terms`，由后续 Weaver 补入。

## 数字与输出证据规则（一票否决）

`Lectures/L01-word-vectors/code-capsules/softmax-probability.md` 里出现的每一个数值、排名、表格项、最近邻结果、cosine/probability/loss/shape/stdout 摘要，必须来自本轮真实输出文件或 run-log。不能根据记忆、示例、旧稿、模型常识、教程文本补数字。

每个 capsule 小节必须包含一个简短证据块：

```yaml
numeric_provenance:
  run_log: Labs/L01-word-vectors/run-log.md#run_id <RUN_ID>
  stdout: Labs/L01-word-vectors/outputs/softmax-probability-stdout.txt
  checked_values:
    - claim: "文档中写出的值或结论"
      source: "stdout/output 文件中的对应行或表格"
      status: copied_from_output
```

如果某个希望讲的数值没有真实输出，就写"本 capsule 未产生该数值"，不要补。若修改代码重新产生输出，必须同步更新 `.py`、`.ipynb`、outputs、run-log、partial 正文，不能只改正文。

## 提交前自检

送审前必须自检并在执行报告写结果：

- 本 capsule partial 必须存在于 `Lectures/L01-word-vectors/code-capsules/softmax-probability.md`，heading 为 `## softmax-probability ... {#softmax-probability}`。
- 本 capsule 的 `.py`、`.ipynb`、stdout/output、run-log、Colab 链接和正文小节路径都存在。
- `Labs/L01-word-vectors/run-log.md` 中必须有唯一一个匹配当前任务的 section：`task_id: <当前task_id>`、`capsule_slug: softmax-probability`、`run_id: <timestamp>__<task_id>__softmax-probability`，且包含 `command / exit_code / stdout / outputs`。
- 本 capsule 正文中的关键数值能在 stdout/output/run-log 中找到。
- `check_links_headings.py` 对本 capsule partial 和 `00-concept-glossary.md` 返回 0 errors；不要把整个 `Labs/L01-word-vectors` 或其他 sibling capsule 当作本 capsule 的自检范围。

## Colab 发布（GitHub 路径）

工具 `/workspace/study-ops/study-ops`，默认 dry-run，`--apply` 才真执行。owner=`dafengbaocy`，repo=`cs224n-study`（公开仓库，已存在则跳过建仓）。

1. `study-ops github-status`
2. 确认公开仓库存在：`/workspace/study-ops/study-ops github-create-repo --name cs224n-study --description "CS224N public study workspace" --public --apply`
3. push（必须加锁，避免多个 capsule 同时推同一个 repo/branch）：`flock /workspace/cs224n-study/artifacts/github-push.lock -c '/workspace/study-ops/study-ops github-push --path Labs/L01-word-vectors --repo dafengbaocy/cs224n-study --branch main --message "Add softmax-probability capsule" --colab-path L01-word-vectors/softmax-probability.ipynb --apply'`
4. 链接：`study-ops github-colab-url --repo dafengbaocy/cs224n-study --branch main --path L01-word-vectors/softmax-probability.ipynb`
5. 把返回的 colab 链接嵌进 `Lectures/L01-word-vectors/code-capsules/softmax-probability.md`。

发布失败写 blocker + 本地 `.ipynb` + run-log 替代，不伪造链接，不泄露 token。

## 教学数据真实性

toy 数据演示的差异必须在输出里清晰可见，不能因取值极端或四舍五入抹平要演示的对比。

## 输出

- `Labs/L01-word-vectors/softmax-probability.py`
- `Labs/L01-word-vectors/softmax-probability.ipynb`
- `Labs/L01-word-vectors/outputs/*`（图表，文件名含语义）
- `Labs/L01-word-vectors/run-log.md`（共享日志；每个 capsule 用 `flock` 原子追加自己的 tagged section）
- `Lectures/L01-word-vectors/code-capsules/softmax-probability.md`（单 capsule partial，含 Colab 链接）
- `Lectures/L01-word-vectors/media-registry/code-capsule-softmax-probability.json`（正文嵌入图片必须登记 local_path ↔ remote_url ↔ run-log provenance；未嵌入正文的本地调试/替代图由 run-log 记录）
- `artifacts/2026-06-07-code-capsule-L01-word-vectors-softmax-probability.md`（执行报告）

## Run-log 防伪造

记录 timestamp / hostname / command / exit code / stdout / stderr / 输出路径。无错误也写为什么简单到无错误、是否跳过 cell、是否用缓存。

共享 run-log 的写入格式必须类似：

```bash
TASK_ID="<当前 Kanban task id>"
RUN_ID="$(date -u +%Y%m%dT%H%M%SZ)__${TASK_ID}__softmax-probability"
RUN_ENTRY="$(mktemp)"
cat > "$RUN_ENTRY" <<EOF
## run_id $RUN_ID

task_id: $TASK_ID
capsule_slug: softmax-probability
timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)
hostname: $(hostname)
command: python Labs/L01-word-vectors/softmax-probability.py
exit_code: <真实退出码>
stdout: Labs/L01-word-vectors/outputs/softmax-probability-stdout.txt
stderr: Labs/L01-word-vectors/outputs/softmax-probability-stderr.txt
outputs: Labs/L01-word-vectors/outputs/softmax-probability-...
notebook: Labs/L01-word-vectors/softmax-probability.ipynb
EOF
mkdir -p Labs/L01-word-vectors
touch Labs/L01-word-vectors/run-log.md
flock Labs/L01-word-vectors/run-log.lock sh -c 'cat "$1" >> Labs/L01-word-vectors/run-log.md' sh "$RUN_ENTRY"
```

禁止多个 worker 直接 `>> Labs/L01-word-vectors/run-log.md`。`flock` 失败或不可用时必须 block，不要冒险并发写共享日志。

## 禁止

不只给 `python foo.py` 没输出 / 不贴未运行代码 / 不编造 stdout / 不编造或沿用旧数值 / 不把 assignment 答案包成 capsule / 不把 smoke 当正式 capsule / 图片不用本地路径冒充已上传 / 有图却不写 media registry 或不上传图床 / 不伪造 Colab 链接 / 不泄露 token / 不写或修改 `Lectures/L01-word-vectors/04-code-capsules.md` / 不在 `Labs/L01-word-vectors/` 下创建 `04-code-capsules.md` 入口副本 / 不用短 wikilink 跨目录引用 glossary。

## 完成状态

正常完成生产时请停在 `review-required`（Kanban 上用 `blocked` + summary/comment 写 `review-required`），并在 summary / 执行报告中写 `ready_for_review: true` 与 `downstream_allowed: false`。这只表示"可送独立 reviewer"，不是自称通过。

不允许把 worker 任务直接标成 `done` / `completed`。`done` 只能由编排器在独立 reviewer `VERDICT: PASS` 后写入。

**终态执行命令（硬规则）**：产物写完、验证命令跑完、执行报告写完后，你必须先确认当前 Kanban task id，然后执行：

```bash
hermes kanban --board cs224n-study block <当前task_id> "review-required: ready_for_review=true downstream_allowed=false; outputs=<主要产物路径>"
```

命令成功后才可以结束回复。禁止用 `complete` / `done` / `completed` 结束 worker 任务；如果无法确认当前 task id，就在最终回复中明确 `protocol_blocker: cannot_determine_task_id`，不要自称完成。

只有遇到真实外部 blocker、越界风险、代码无法真实运行或发布链路无法给出可验证替代时，才使用 `blocked` / `quarantined`。不允许自称 pass，不允许 `downstream_allowed: true`。

## 完成回报

task id / parent / changed files / capsule 概念 / 真实输出摘要 / Colab 链接或 blocker / run-log 路径 / `ready_for_review: true` / `downstream_allowed: false`
